#!/usr/bin/env bash

exec 2> >(logger -s -t $(basename $0))

set -e

use_sudo=
if [ $UID -ne 0 ]; then
    use_sudo="sudo"
fi

$use_sudo systemctl stop $($use_sudo /opt/server/bin/service-scripts/get-full-service-name $1)

exit 0