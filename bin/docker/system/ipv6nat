#!/usr/bin/env bash

set -e

docker pull robbertkl/ipv6nat

docker create \
--name ipv6nat \
--cap-add NET_ADMIN \
--cap-add SYS_MODULE \
--network host \
-v /var/run/docker.sock:/var/run/docker.sock:ro \
robbertkl/ipv6nat