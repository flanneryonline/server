#!/usr/bin/env bash

exec 2> >(logger -s -t $(basename $0))

set -e

check_service=$1

find_service=$(find /opt/server/bin/docker/ -type f -exec echo {} \; | grep $check_service)
[ ! -z $find_service ] && exit 0

exit 1