#!/usr/bin/env bash

exec 2> >(logger -s -t $(basename $0))

set -e

network=$1
service=$2
[ network = "default" ] && exit 0

if [ network = "vpn" ]
then

    systemctl status fods@media-vpn.service >/dev/null || systemctl enable /opt/server/system/systemd/docker/fods@media-vpn.service

fi